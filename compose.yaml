services:
  agent_ventas:
    build:
      context: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
